// Preparing bot
function readyBot(client) {
    console.log(`${client.user.tag} is ready`);
};

// Greet user
function greetUser(member) {
    
}

// Exporting modules
module.exports = {
    readyBot
};